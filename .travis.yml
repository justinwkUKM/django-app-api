language: python
python:
    - "3.6"

services:
    - docker

before_script: pip install docker-compose

script:
    - docker-compose run app sh -c "python manage.py test && flake8"

notifications:
    email:
        recipients:
            - waqas@siswa.ukm.edu.my
        on_success: always
        on_failure: always
